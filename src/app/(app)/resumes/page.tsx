import { PhasePlaceholder } from "@/components/phase-placeholder";

export default function ResumesPage() {
  return (
    <PhasePlaceholder
      title="Resumes"
      description="Upload a master resume, parse it into your Career DNA, and generate tailored, truthful versions per job with side-by-side comparison and approval."
      phase="Phase 2 (Resume Intelligence) and Phase 5 (Resume Tailoring)"
    />
  );
}
