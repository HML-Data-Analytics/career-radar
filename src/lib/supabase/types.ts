/**
 * Placeholder Supabase database types. Regenerate against the live project:
 *
 *   npx supabase gen types typescript --project-id <project-id> > src/lib/supabase/types.ts
 *
 * Using `any` here keeps the app buildable before a Supabase project is
 * linked; the generated file replaces this entirely.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
